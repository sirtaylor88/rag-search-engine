# Constants

```{eval-rst}
.. automodule:: cli.constants
   :members:
   :undoc-members:

.. seealso::

   :doc:`/api/schemas` — payload models that consume constants such as ``SEARCH_LIMIT`` and ``DEFAULT_ALPHA``

   :doc:`/api/inverted_index` — uses ``BM25_K1``, ``BM25_B``, and ``CACHE_DIR_PATH``

   :doc:`/api/semantic_search` — uses ``DEFAULT_EMBEDDING_MODEL`` and ``CHUNK_SIZE``
```
