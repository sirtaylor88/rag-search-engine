# Inverted Index

```{eval-rst}
.. automodule:: cli.core.keyword_search
   :members:
   :undoc-members:
   :show-inheritance:

.. seealso::

   :doc:`/api/keyword_search_cli` — CLI entry point that dispatches to index commands

   :doc:`/api/commands/build` — ``BuildCommand`` that calls ``build`` and ``save``

   :doc:`/api/commands/search/keyword_search_commands` — ``SearchCommand`` and ``BM25SearchCommand``

   :doc:`/api/singleton` — ``Singleton`` base class used by ``InvertedIndex``

   :doc:`/api/utils` — ``tokenize_text`` and ``get_stemmed_tokens`` used during indexing
```
