# Embed Commands

```{eval-rst}
.. automodule:: cli.commands.embed_commands
   :members:
   :undoc-members:
   :show-inheritance:

.. seealso::

   :doc:`/api/semantic_search_cli` — CLI entry point that registers ``embed_text``, ``embed_query``, and ``embed_chunks``

   :doc:`/api/semantic_search` — ``SemanticSearch`` and ``ChunkedSemanticSearch`` called by embed commands
```
