# TF Command

```{eval-rst}
.. autoclass:: cli.commands.compute.tf_commands.ComputeTFCommand
   :members:
   :show-inheritance:

.. seealso::

   :doc:`/api/keyword_search_cli` — CLI entry point that registers the ``tf`` subcommand

   :doc:`/api/inverted_index` — ``InvertedIndex.get_tf`` called by this command
```
