# TF-IDF Command

```{eval-rst}
.. autoclass:: cli.commands.compute.tf_commands.ComputeTFIDFCommand
   :members:
   :show-inheritance:

.. seealso::

   :doc:`/api/keyword_search_cli` — CLI entry point that registers the ``tfidf`` subcommand

   :doc:`/api/inverted_index` — ``InvertedIndex.get_tf`` and ``InvertedIndex.get_idf`` called by this command
```
