# Normalize Command

```{eval-rst}
.. automodule:: cli.commands.compute.normalize_command
   :members:
   :undoc-members:
   :show-inheritance:

.. seealso::

   :doc:`/api/hybrid_search_cli` — CLI entry point that registers the ``normalize`` subcommand

   :doc:`/api/hybrid_search` — ``normalize_scores`` that underpins the weighted-search pipeline
```
