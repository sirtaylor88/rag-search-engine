# IDF Command

```{eval-rst}
.. autoclass:: cli.commands.compute.idf_commands.ComputeIDFCommand
   :members:
   :show-inheritance:

.. seealso::

   :doc:`/api/keyword_search_cli` — CLI entry point that registers the ``idf`` subcommand

   :doc:`/api/inverted_index` — ``InvertedIndex.get_idf`` called by this command
```
