# Semantic Search

```{eval-rst}
.. automodule:: cli.core.semantic_search
   :members:
   :undoc-members:
   :show-inheritance:

.. seealso::

   :doc:`/api/semantic_search_cli` — CLI entry point that dispatches to semantic search commands

   :doc:`/api/commands/search/semantic_search_command` — ``SemanticSearchCommand`` and ``SearchChunkedCommand``

   :doc:`/api/commands/verify` — ``VerifyCommand`` and ``VerifyEmbeddingsCommand``

   :doc:`/api/singleton` — ``Singleton`` base class used by ``SemanticSearch``

   :doc:`/api/utils` — ``cosine_similarity`` and ``get_sentences`` used during search and chunking
```
