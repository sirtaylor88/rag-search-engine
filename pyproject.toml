[project]
name = "rag-search-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.9"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "pylint>=4.0.5",
    "ruff>=0.15.12",
]
